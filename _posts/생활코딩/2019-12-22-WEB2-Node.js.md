---
layout: post
title: "WEB2 Node.js"
tags: 
    - 생활코딩
comments: true
---

**Node.js**
* 2008년 구글의 V8엔진을 기반으로 하는 Node.js 탄생

# 9. URL의 이해
* http://opentutorial.org:3000/main?id=HTML&page=12
* 마지막 ? 뒤 부터가 query string인데 웹서버에게 데이터를 전달할 수 있음
* 

# 10. URL을 통해서 입력된 값 사용하기
* 쿼리 스트링에 따라 다른 정보를 보여주기
* request.url을 통해 url 주소를 알아낼 수 있음
* 또한 url.parse(request.url, true).query를 통해 쿼리 스트링 값 알 수 있음
```javascript
var queryData = url.parse(_url, true).query;
console.log(queryData);
// { id: 'HTML' }
```

# 12. Node.js 의 파일 읽기 기능
* CRUD(Create Read Update Delete)
* 생성과 읽기는 매우 중요
* file을 node에서 읽는 방법
```javascript
var fs = require('fs');

fs.readFile('sample.txt', 'utf8', function(err, data) {
    console.log(data);
});

// console에서 node로 실행
```
* 동적으로 처리하기 위해 리터럴 템플릿을 이용하여 리터럴 템플릿 내에 동적으로 파일을 읽어 올 수있도록 처리하고, 템플릿을 response.end(템플릿) 으로 응답하는 형태로 처리함

# 19.1 App제작 - Not Found 구현
* 요청한 url에 따라 처리가 달라짐, if문 사용
* 정상적인 url로 접속시에는 200과 처리해줄 내용을 end에 넣어 응답
* 없는 url은 404와 오류 메시지 처리

```javascript
  if (pathName === '/') {
    fs.readFile(`data/${queryData.id}`, 'utf8', function (err, description) {
      var template = `
        <!doctype html>
        <html>
        <head>
        <title>WEB1 - ${title}</title>
        <meta charset="utf-8">
        </head>
        <body>
        <h1><a href="/">WEB</a></h1>
        <ul>
        <li><a href="?id=HTML">HTML</a></li>
        <li><a href="?id=CSS">CSS</a></li>
        <li><a href="?id=JavaScript">JavaScript</a></li>
        </ul>
        <h2>${title}</h2>
        <p>${description}</p>
        </body>
        </html>
        `;
      response.writeHead(200);
      response.end(template);
    });
  } else {
    response.writeHead(404);
    response.end('Not Found');
  }
```

# 19.2 App제작 - 홈페이지 구현
* 요청 url이 홈인 경우도 처리하기


# 23 Node.js에서 파일목록 알아내기
* 특정 폴더의 파일 리스트를 알 수 있는 방법
* fs.readdir()를 이용
```javascript
var testFolder = './data';
var fs = require('fs');

fs.readdir(testFolder, function(error, Filelist) {
    console.log(Filelist);
});

//[ 'CSS', 'HTML', 'JavaScript' ]
```

# 24 App제작 - 글목록 출력하기
여기서 부터 다시 보기



```javascript

```